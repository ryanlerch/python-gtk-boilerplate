#!/usr/bin/env python3

import sys
sys.path.insert(1, '@pythondir@')
sys.path.insert(1, '@pyexecdir@')

from sampleapp.sampleapp import MyApplication

if __name__ == "__main__":
    app = MyApplication()
    exit_status = app.run(sys.argv)
    sys.exit(exit_status)
